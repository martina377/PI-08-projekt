@model IEnumerable<PI08_aplikacija.Models.Farma>

@{
    ViewBag.Title = "Index";
}

<h2>Farma</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Farma1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lokacija)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vlasnik)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Farma1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Lokacija)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vlasnik)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Farma }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Farma }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Farma })
        </td>
    </tr>
}

</table>
