@model IEnumerable<PI08_aplikacija.Models.Hrana_Životinja>

@{
    ViewBag.Title = "Index";
}

<h2>Hrana_životinje</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Hrana.Vrsta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Životinja.Ime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Hrana.Vrsta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Životinja.Ime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
