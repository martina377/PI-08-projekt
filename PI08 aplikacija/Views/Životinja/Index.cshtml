@model IEnumerable<PI08_aplikacija.Models.Životinja>

@{
    ViewBag.Title = "Index";
}

<h2>Životinja</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vrsta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Proizvodi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Označena)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vrsta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Proizvodi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Označena)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Životinje }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Životinje }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Životinje })
        </td>
    </tr>
}

</table>
